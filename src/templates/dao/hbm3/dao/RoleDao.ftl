package ${project.org}.dao;

import ${project.org}.common.dao.BaseDao;
import ${project.org}.domain.Role;

/**
 * User: joeytang
 * Date: ${project.currentTime}
 * 角色dao接口
 */
public interface RoleDao extends BaseDao<Role, ${project.security.idType2ShortJavaType}> {
}

