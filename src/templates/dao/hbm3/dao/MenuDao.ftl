package ${project.org}.dao;

import ${project.org}.common.dao.BaseDao;
import ${project.org}.domain.Menu;

/**
 * User: joeytang
 * Date: ${project.currentTime}
 * 菜单dao接口
 */
public interface MenuDao extends BaseDao<Menu, ${project.security.idType2ShortJavaType}> {
}