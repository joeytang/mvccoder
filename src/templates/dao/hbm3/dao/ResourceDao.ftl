package ${project.org}.dao;

import ${project.org}.common.dao.BaseDao;
import ${project.org}.domain.Resource;

/**
 * User: joeytang
 * Date: ${project.currentTime}
 * 资源dao接口
 */
public interface ResourceDao extends BaseDao<Resource, ${project.security.idType2ShortJavaType}> {
}

