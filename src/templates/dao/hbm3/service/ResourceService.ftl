package ${project.org}.service;

import ${project.org}.domain.Resource;
import ${project.org}.common.service.BaseService;

/**
 * User: joeytang
 * Date: ${project.currentTime}
 * 资源业务逻辑接口
 */
public interface ResourceService  extends BaseService<Resource,${project.security.idType2ShortJavaType}> {
}