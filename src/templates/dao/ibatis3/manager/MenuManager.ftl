package ${project.org}.security.manager;

import ${project.org}.security.domain.Menu;

/**
 * User: joeytang
 * Date: ${project.currentTime}
 * 菜单业务逻辑接口
 */
public interface MenuManager extends BaseManager<Menu,Long> {
}

