package ${project.org}.security.manager;

import ${project.org}.security.domain.Resource;

/**
 * User: joeytang
 * Date: ${project.currentTime}
 * 资源业务逻辑接口
 */
public interface ResourceManager  extends BaseManager<Resource,Long> {
}